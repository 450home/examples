spec: v0.6

runtime: base-compat:latest

rootfs:
  source: ./Dockerfile
  type: erofs

cmd: [
  "/usr/bin/minio",
  "server",
  "--address", "0.0.0.0:9000",
  "--console-address", "0.0.0.0:9001",
  "/data"
]

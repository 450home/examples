spec: v0.7

runtime: base-compat:latest

targets:
  - kraftcloud/x86_64

rootfs:
  source: ./Dockerfile
  format: erofs

cmd: ["/usr/bin/skipper", "-address", ":9090", "-routes-file", "/etc/skipper/example.eskip"]

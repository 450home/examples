spec: v0.7

runtime: base-compat:latest

targets:
  - kraftcloud/x86_64

rootfs:
  source: ./Dockerfile
  format: erofs

cmd: [ "/app/code-server/bin/code-server", "--host", "0.0.0.0", "--port", "8443", "--auth", "password" ]
